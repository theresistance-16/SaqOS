This Repository Contains SaqOs source code. Version one of SaqOS is just the bootloader, no kernel no nothing, version 2 will be the bootloader but the user can type inputs. all of this is written in 16 bit assembly
i might rewrite everything in 32 bit assembly and make the bootloader switch from 16 to 32 bit 
